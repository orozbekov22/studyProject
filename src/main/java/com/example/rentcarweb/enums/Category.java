package com.example.rentcarweb.enums;

public enum Category {
    SUV, // A Sport Utility Vehicle(SUV) is defined by its off-road capabilities and roominess.
    HATCHBACK,
    CROSSOVER,
    SEDAN,
    SPORT_CAR,
    COUPE,
    MINIVAN,
    PICKUP
}
